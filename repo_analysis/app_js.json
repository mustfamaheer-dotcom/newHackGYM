{
  "code": 200,
  "data": {
    "description": "",
    "html": "<html><head><meta name=\"color-scheme\" content=\"light dark\"></head><body><pre style=\"word-wrap: break-word; white-space: pre-wrap;\">// ===========================================================================\n// ZKTeco Attendance Dashboard - Frontend Logic\n// ===========================================================================\n\nconst API_BASE = window.location.origin;\nconst SIGNALR_URL = `${API_BASE}/signalr`;\n\nlet signalrConn = null;\n\n// ---------------------------------------------------------------------------\n// Initialize\n// ---------------------------------------------------------------------------\n$(document).ready(() => {\n    initTime();\n    initMonthYearSelectors();\n    initSignalR();\n\n    loadDashboardStats();\n    loadTodayAttendance();\n    loadUsers();\n    loadDevices();\n\n    // Refresh stats periodically\n    setInterval(loadDashboardStats, 30000);\n    setInterval(loadTodayAttendance, 60000);\n});\n\nfunction initTime() {\n    setInterval(() => {\n        $('#currentTime').text(new Date().toLocaleString());\n    }, 1000);\n}\n\nfunction initMonthYearSelectors() {\n    const now = new Date();\n    for (let m = 1; m <= 12; m++) {\n        $('#reportMonth').append(`<option value=\"${m}\" ${m === now.getMonth()+1 ? 'selected' : ''}>${m}</option>`);\n    }\n    for (let y = now.getFullYear() - 2; y <= now.getFullYear() + 1; y++) {\n        $('#reportYear').append(`<option value=\"${y}\" ${y === now.getFullYear() ? 'selected' : ''}>${y}</option>`);\n    }\n}\n\n// ---------------------------------------------------------------------------\n// SignalR\n// ---------------------------------------------------------------------------\nfunction initSignalR() {\n    signalrConn = $.hubConnection(SIGNALR_URL, { useDefaultPath: false });\n    var hub = signalrConn.createHubProxy('attendanceHub');\n\n    hub.on('attendanceRecorded', function (record) {\n        prependRecord(record);\n        showToast('New ' + record.status + ' by ' + (record.userName || record.employeeId), 'success');\n        loadDashboardStats();\n    });\n\n    hub.on('recordsSynced', function (info) {\n        showToast('Synced ' + info.inserted + ' records from ' + info.deviceSerial, 'success');\n        loadTodayAttendance();\n        loadDashboardStats();\n    });\n\n    hub.on('clientCountChanged', function (count) {\n        $('#clientCount').text(count);\n    });\n\n    signalrConn.start()\n        .done(function () { updateConnectionStatus(true); })\n        .fail(function () {\n            updateConnectionStatus(false);\n            setTimeout(initSignalR, 5000);\n        });\n}\n\nfunction updateConnectionStatus(connected) {\n    const badge = $('#connectionStatus');\n    if (connected) {\n        badge.removeClass('badge-danger').addClass('badge-success').html(\n            '<i class=\"fas fa-circle text-light mr-1\" style=\"font-size: 8px;\"></i> Live'\n        );\n    } else {\n        badge.removeClass('badge-success').addClass('badge-danger').html(\n            '<i class=\"fas fa-circle text-light mr-1\" style=\"font-size: 8px;\"></i> Reconnecting...'\n        );\n    }\n}\n\n// ---------------------------------------------------------------------------\n// Dashboard stats\n// ---------------------------------------------------------------------------\nasync function loadDashboardStats() {\n    try {\n        const res = await fetch(`${API_BASE}/api/attendance/dashboard-stats`);\n        const json = await res.json();\n        if (json.success) {\n            const d = json.data;\n            $('#kpiTotalUsers').text(d.totalActiveUsers);\n            $('#kpiCheckedIn').text(d.checkedInToday);\n            $('#kpiLate').text(d.lateToday);\n            $('#kpiAbsent').text(d.absentToday);\n        }\n    } catch (e) {\n        console.error('Dashboard stats error:', e);\n    }\n}\n\n// ---------------------------------------------------------------------------\n// Today attendance\n// ---------------------------------------------------------------------------\nasync function loadTodayAttendance() {\n    try {\n        const res = await fetch(`${API_BASE}/api/attendance/today`);\n        const json = await res.json();\n        if (json.success) {\n            renderTodayRecords(json.data);\n        }\n    } catch (e) {\n        console.error('Today attendance error:', e);\n        $('#todayRecordsBody').html('<tr><td colspan=\"7\" class=\"text-center text-danger py-4\">Failed to load</td></tr>');\n    }\n}\n\nfunction renderTodayRecords(records) {\n    if (!records || records.length === 0) {\n        $('#todayRecordsBody').html('<tr><td colspan=\"7\" class=\"text-center text-muted py-4\">No attendance records today</td></tr>');\n        return;\n    }\n    const rows = records.map(r => `\n        <tr>\n            <td>${new Date(r.timestamp).toLocaleTimeString()}</td>\n            <td>${r.employeeId || '-'}</td>\n            <td>${r.userName || '-'}</td>\n            <td>${r.department || '-'}</td>\n            <td><span class=\"badge ${r.status === 'CheckIn' ? 'badge-checkin' : 'badge-checkout'}\">${r.status}</span></td>\n            <td>${r.verificationType} (${r.verificationScore}%)</td>\n            <td>${r.deviceSerialNumber || '-'}</td>\n        </tr>\n    `).join('');\n    $('#todayRecordsBody').html(rows);\n}\n\nfunction prependRecord(record) {\n    const row = `\n        <tr class=\"record-row-new\">\n            <td>${new Date(record.timestamp).toLocaleTimeString()}</td>\n            <td>${record.employeeId || '-'}</td>\n            <td>${record.userName || '-'}</td>\n            <td>-</td>\n            <td><span class=\"badge ${record.status === 'CheckIn' ? 'badge-checkin' : 'badge-checkout'}\">${record.status}</span></td>\n            <td>${record.verificationType}</td>\n            <td>-</td>\n        </tr>`;\n    $('#todayRecordsBody').prepend(row);\n}\n\n// ---------------------------------------------------------------------------\n// Users\n// ---------------------------------------------------------------------------\nasync function loadUsers() {\n    try {\n        const res = await fetch(`${API_BASE}/api/users`);\n        const json = await res.json();\n        if (json.success) {\n            renderUsers(json.data);\n        }\n    } catch (e) {\n        console.error('Users load error:', e);\n    }\n}\n\nfunction renderUsers(users) {\n    if (!users || users.length === 0) {\n        $('#usersBody').html('<tr><td colspan=\"9\" class=\"text-center text-muted py-4\">No users</td></tr>');\n        return;\n    }\n    $('#usersBody').html(users.map(u => `\n        <tr>\n            <td>${u.userId}</td>\n            <td>${u.name}</td>\n            <td>${u.employeeId}</td>\n            <td>${u.department || '-'}</td>\n            <td>${u.position || '-'}</td>\n            <td>${u.email || '-'}</td>\n            <td>${u.hasBiometric ? '<i class=\"fas fa-check text-success\"></i>' : '<i class=\"fas fa-times text-muted\"></i>'}</td>\n            <td>${u.status}</td>\n            <td>\n                <button class=\"btn btn-sm btn-outline-primary\" onclick=\"editUser(${u.userId})\"><i class=\"fas fa-edit\"></i></button>\n                <button class=\"btn btn-sm btn-outline-danger\" onclick=\"deleteUser(${u.userId})\"><i class=\"fas fa-trash\"></i></button>\n            </td>\n        </tr>\n    `).join(''));\n}\n\nfunction openCreateUser() {\n    $('#userModalTitle').text('Add User');\n    $('#userId').val('');\n    $('#userName, #userEmployeeId, #userEmail, #userPhone, #userDepartment, #userPosition').val('');\n}\n\nasync function editUser(id) {\n    try {\n        const res = await fetch(`${API_BASE}/api/users/${id}`);\n        const json = await res.json();\n        if (json.success) {\n            const u = json.data;\n            $('#userModalTitle').text('Edit User');\n            $('#userId').val(u.userId);\n            $('#userName').val(u.name);\n            $('#userEmployeeId').val(u.employeeId);\n            $('#userEmail').val(u.email);\n            $('#userPhone').val(u.phoneNumber);\n            $('#userDepartment').val(u.department);\n            $('#userPosition').val(u.position);\n            $('#userModal').modal('show');\n        }\n    } catch (e) {\n        showToast('Failed to load user', 'error');\n    }\n}\n\nasync function saveUser() {\n    const id = $('#userId').val();\n    const payload = {\n        name: $('#userName').val(),\n        employeeId: $('#userEmployeeId').val(),\n        email: $('#userEmail').val(),\n        phoneNumber: $('#userPhone').val(),\n        department: $('#userDepartment').val(),\n        position: $('#userPosition').val()\n    };\n    try {\n        const url = id ? `${API_BASE}/api/users/${id}` : `${API_BASE}/api/users`;\n        const method = id ? 'PUT' : 'POST';\n        const res = await fetch(url, {\n            method,\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify(payload)\n        });\n        const json = await res.json();\n        if (json.success) {\n            $('#userModal').modal('hide');\n            showToast(id ? 'User updated' : 'User created', 'success');\n            loadUsers();\n        } else {\n            showToast(json.message || 'Failed', 'error');\n        }\n    } catch (e) {\n        showToast('Network error', 'error');\n    }\n}\n\nasync function deleteUser(id) {\n    if (!confirm('Delete this user? Records will be preserved (soft delete).')) return;\n    try {\n        const res = await fetch(`${API_BASE}/api/users/${id}`, { method: 'DELETE' });\n        const json = await res.json();\n        if (json.success) {\n            showToast('User deleted', 'success');\n            loadUsers();\n        } else {\n            showToast(json.message || 'Failed', 'error');\n        }\n    } catch (e) {\n        showToast('Network error', 'error');\n    }\n}\n\n// ---------------------------------------------------------------------------\n// Reports\n// ---------------------------------------------------------------------------\nasync function loadMonthlyReport() {\n    const month = $('#reportMonth').val();\n    const year = $('#reportYear').val();\n    try {\n        const res = await fetch(`${API_BASE}/api/attendance/report/monthly?year=${year}&month=${month}`);\n        const json = await res.json();\n        if (json.success) {\n            renderReport(json.data);\n        }\n    } catch (e) {\n        showToast('Failed to load report', 'error');\n    }\n}\n\nfunction renderReport(items) {\n    if (!items || items.length === 0) {\n        $('#reportBody').html('<tr><td colspan=\"10\" class=\"text-center text-muted py-4\">No data for selected period</td></tr>');\n        return;\n    }\n    $('#reportBody').html(items.map(r => `\n        <tr>\n            <td>${r.employeeId}</td>\n            <td>${r.name}</td>\n            <td>${r.department || '-'}</td>\n            <td class=\"text-success\"><strong>${r.presentDays}</strong></td>\n            <td class=\"text-warning\">${r.lateDays}</td>\n            <td class=\"text-danger\">${r.absentDays}</td>\n            <td>${r.earlyLeaveDays}</td>\n            <td>${r.halfDays}</td>\n            <td>${r.onLeaveDays}</td>\n            <td>${r.totalWorkHours.toFixed(2)}h</td>\n        </tr>\n    `).join(''));\n}\n\n// ---------------------------------------------------------------------------\n// Devices\n// ---------------------------------------------------------------------------\nasync function loadDevices() {\n    try {\n        const res = await fetch(`${API_BASE}/api/devices`);\n        const json = await res.json();\n        if (json.success) {\n            renderDevices(json.data);\n        }\n    } catch (e) {\n        console.error('Devices load error:', e);\n    }\n}\n\nfunction renderDevices(devices) {\n    if (!devices || devices.length === 0) {\n        $('#devicesBody').html('<tr><td colspan=\"8\" class=\"text-center text-muted py-4\">No devices registered</td></tr>');\n        return;\n    }\n    $('#devicesBody').html(devices.map(d => `\n        <tr>\n            <td><strong>${d.serialNumber}</strong></td>\n            <td>${d.model}</td>\n            <td>${d.ipAddress}</td>\n            <td>${d.port}</td>\n            <td>${d.location || '-'}</td>\n            <td><span class=\"badge badge-${d.status.toLowerCase()}\">${d.status}</span></td>\n            <td>${new Date(d.lastSyncedDate).toLocaleString()}</td>\n            <td>\n                <button class=\"btn btn-sm btn-outline-danger\" onclick=\"deleteDevice(${d.id})\"><i class=\"fas fa-trash\"></i></button>\n            </td>\n        </tr>\n    `).join(''));\n}\n\nfunction openCreateDevice() {\n    $('#deviceSerial, #deviceIp, #deviceLocation').val('');\n    $('#deviceModel').val('MB2000');\n    $('#devicePort').val(4370);\n}\n\nasync function saveDevice() {\n    const payload = {\n        serialNumber: $('#deviceSerial').val(),\n        model: $('#deviceModel').val(),\n        firmwareVersion: '1.0',\n        ipAddress: $('#deviceIp').val(),\n        port: parseInt($('#devicePort').val()),\n        location: $('#deviceLocation').val()\n    };\n    try {\n        const res = await fetch(`${API_BASE}/api/devices`, {\n            method: 'POST',\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify(payload)\n        });\n        const json = await res.json();\n        if (json.success) {\n            $('#deviceModal').modal('hide');\n            showToast('Device registered', 'success');\n            loadDevices();\n        } else {\n            showToast(json.message || 'Failed', 'error');\n        }\n    } catch (e) {\n        showToast('Network error', 'error');\n    }\n}\n\nasync function deleteDevice(id) {\n    if (!confirm('Remove this device?')) return;\n    try {\n        await fetch(`${API_BASE}/api/devices/${id}`, { method: 'DELETE' });\n        showToast('Device removed', 'success');\n        loadDevices();\n    } catch (e) {\n        showToast('Network error', 'error');\n    }\n}\n\n// ---------------------------------------------------------------------------\n// Toast\n// ---------------------------------------------------------------------------\nfunction showToast(message, type = '') {\n    $('#toast').removeClass('success error').addClass(type).text(message).addClass('show');\n    setTimeout(() => $('#toast').removeClass('show'), 3500);\n}\n</pre></body></html>",
    "httpStatus": 200,
    "httpStatusText": "",
    "title": "",
    "url": "https://raw.githubusercontent.com/mustfamaheer-dotcom/TestDevice/main/frontend/js/app.js",
    "usage": {
      "tokens": 3054
    }
  },
  "meta": {
    "usage": {
      "tokens": 3054
    }
  },
  "status": 20000
}