using System; using System.Collections.Generic; using System.Linq; using System.Web.Http; using AttendanceSystem.Filters; using Microsoft.AspNet.SignalR; using Microsoft.Owin; using Owin; [assembly: OwinStartup(typeof(AttendanceSystem.Startup))] namespace AttendanceSystem { public class Startup { public void Configuration(IAppBuilder app) { // SignalR only — Web API is configured via GlobalConfiguration in Global.asax.cs var hubConfig = new HubConfiguration { EnableDetailedErrors = true, EnableJavaScriptProxies = true }; app.MapSignalR("/signalr", hubConfig); } } }